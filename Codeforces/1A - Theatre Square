#include <iostream>
using namespace std;

int main()
{
    int n, m, a;
    long long x, y, sum;
    cin >> n >> m >> a;
    
    x = n / a;
    y = m / a;
    
    if (n % a != 0)
        x++;
    if (m % a != 0)
        y++;
        
    sum = x * y;
    cout << sum;
    
    return 0;
}
